{
  "name": "@ai-agents-observability/redaction",
  "version": "1.5.0",
  "private": true,
  "description": "Regex scrubber — client-side redaction before transcript upload",
  "engines": {
    "node": ">=26"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "bench": "vitest bench",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "@types/node": "26.4.0",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "fast-check": "catalog:"
  }
}
